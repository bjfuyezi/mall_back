<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserProductMapper">
    <select id="selectByUser" parameterType="Integer" resultType="Integer">
        SELECT product_id FROM user_product WHERE user_id = #{uid}
    </select>
    <select id="selectAllByProductId" parameterType="Integer" resultType="Integer">
        SELECT user_id FROM user_product WHERE product_id = #{id}
    </select>
    <select id="isStar" resultType="Integer">
        SELECT count(*) AS total_records FROM user_product WHERE user_id = #{uid} and product_id = #{pid}
    </select>

    <delete id="deleteStar">
        DELETE FROM user_product WHERE user_id = #{uid} and product_id = #{pid}
    </delete>

    <insert id="insert">
        INSERT INTO user_product (user_id, product_id, created_time)
        VALUES (#{uid}, #{pid}, NOW())
    </insert>
</mapper>